services:
  dashboard:
    image: caddy:2-alpine
    container_name: productivity-dashboard
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./www:/srv/www:ro
